name: saludgest_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.6.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.3.0
  table_calendar: ^3.0.9
  intl: ^0.18.0
  permission_handler: ^12.0.0+1
  flutter_local_notifications: ^19.0.0
  file_picker: ^8.1.3  # Para seleccionar PDFs
  open_file: ^3.5.10  # Para abrir PDFs descargados
  path_provider: ^2.1.5  # Para manejar rutas de almacenamiento local
  http_parser: ^4.0.2
  uuid: ^4.5.0
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true