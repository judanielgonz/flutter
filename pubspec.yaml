name: saludgest_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.6.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.3.0
  table_calendar: ^3.0.9  # Biblioteca para el calendario
  intl: ^0.18.0  # Soporte de idiomas y fechas en español
  permission_handler: ^12.0.0+1 # Para permisos de notificaciones
  flutter_local_notifications: ^19.0.0 # Para notificaciones

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
